# WordPress to MkDocs Migration Plan

Migration of the old Mindwerks.net WordPress site to the new MkDocs (Material theme) static site.

## Prerequisites

- [x] Obtain the WordPress SQL dump (`.sql` file)
- [x] Obtain the WordPress tarball containing `wp-content/uploads/` with images
- [x] Place both files in an accessible location (`/tmp/mindwerks/`)

---

## Phase 1: Inventory & Analysis

### 1.1 Analyze the SQL Dump
- [x] Inspect the SQL dump to identify WordPress table prefix (`mw_`)
- [x] Count and list all published posts (58 posts, 2006–2015)
- [x] Count and list all published pages (17 pages)
- [x] Identify categories and tags
- [x] Extract post metadata: dates, authors, slugs

### 1.2 Analyze the WordPress Files
- [x] Inventory the `wp-content/uploads/` directory
- [x] List all image files (69 originals + 295 thumbnails)
- [x] Images span 2010–2015 in `uploads/YYYY/MM/` structure

### 1.3 Cross-Reference with Wayback Machine
- [x] Determined not needed — SQL dump and tarball were complete

---

## Phase 2: Build the Migration Script

### 2.1 Migration Script (`migrate.py`)
- [x] Parse the SQL dump using state machine approach (MySQL → in-memory parsing)
- [x] Convert WordPress HTML content to Markdown using `markdownify`
- [x] Handle WordPress shortcodes (`[caption]`, `[code]`, `[sourcecode]`)
- [x] Preserve code blocks with language detection
- [x] Generate front matter with dates, categories, and tags

### 2.2 Image Processing
- [x] Copy 69 original images to `docs/assets/images/YYYY/MM/`
- [x] Skip 295 WordPress-generated thumbnails
- [x] Rewrite image URLs in Markdown content to absolute paths (`/assets/images/...`)
- [x] Handle thumbnail URL references (e.g., `image-300x200.jpg` → `image.jpg`)

### 2.3 Content Organization
- [x] Blog posts → `docs/blog/posts/YYYY-MM-DD-slug.md`
- [x] Static pages → `docs/slug.md`
- [x] Skipped Archives and Sitemap pages (auto-generated by MkDocs)

---

## Phase 3: MkDocs Configuration

- [x] Enabled blog plugin (built into mkdocs-material 9.7.1)
- [x] Configured nav: Home, Blog, Projects, Tools, About
- [x] Added markdown extensions (syntax highlighting, admonitions, etc.)
- [x] No extra dependencies needed (blog plugin is built-in)

---

## Phase 4: Migration Results

- [x] 58 blog posts migrated successfully
- [x] 15 static pages migrated (2 skipped: Archives, Sitemap)
- [x] 69 images copied
- [x] Site builds with zero warnings
- [x] Spot-checked posts: proper Markdown, working image references, correct metadata

---

## Phase 5: Remaining Tasks

- [ ] Run `mkdocs serve` and browse the site locally for a full visual review
- [ ] Compare key pages with [Wayback Machine snapshots](https://web.archive.org/web/2022/https://mindwerks.net/) if desired
- [ ] Fix any remaining HTML artifacts or conversion issues found during review
- [ ] Remove `migrate.py` from the repo (or keep for reference)
- [ ] Commit the migrated content
- [ ] Deploy and verify on production
